#include <bits/stdc++.h>
#include <string>
#include<algorithm>
using namespace std;  

int main(){
  long long int n;cin >> n;
  if(n >= pow(2,31) || n < pow(2,31)*-1)cout << "No";
  else cout << "Yes";
  }

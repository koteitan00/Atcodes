#include <bits/stdc++.h>
#include <string>
#include<algorithm>
using namespace std;  

int main(){
  int a,b;
  cin >> a >> b;
  int dif = abs(a-b);
  if(dif == 1 || dif == 9)cout << "Yes";
  else cout << "No";
  }
